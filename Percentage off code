print("Merchandise with percent off.")
price = input("What is the price? ")
percent = input("How much percentage off? ")

new_percent = str("0.") + (percent)
amount_off = float(price) * float(new_percent)
total = float(price) - float(amount_off)

print(round(total, 2))
